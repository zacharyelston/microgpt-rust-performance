[package]
name = "microgpt_rust"
version = "0.1.0"
edition = "2021"

[dependencies]
rand = "0.8"
rayon = { version = "1.7", optional = true }

[features]
default = ["parallel"]
parallel = ["rayon"]

[profile.release]
opt-level = 3
lto = true
